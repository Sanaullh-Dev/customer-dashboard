/**
 * Controllers Index
 * Export all controllers from a single entry point
 */

import * as customerController from './customerController';
import * as authController from './authController';

export { customerController, authController };
